jaffle_shop:
  outputs:

    dev:
      type: postgres
      threads: 1
      host: localhost
      port: 6543
      user: postgres
      pass: postgres
      dbname: backend
      schema: dbt

  target: dev
